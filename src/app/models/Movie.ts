import { Title } from '@angular/platform-browser';

export interface Movie {
    Title?: string,
    Year?: number,
    Runtime?: string,
    Genre?: string,
    Director?: string,
    Poster?: string,
    Imdbid?: string
};